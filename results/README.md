# Version-Controlled Results

This directory contains small, reviewable source-data tables used by public
project figures. Raw logs, datasets, encoded arrays, and checkpoints remain
local because they are large or reproducible artifacts.

Each table is generated from the corresponding validated plotting command and
should retain the metric definition, run metadata, and source-log identity
needed to audit the public claim.
