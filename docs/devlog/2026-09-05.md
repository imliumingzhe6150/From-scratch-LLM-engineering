# Development Log: 2026-09-05

## Session Goal

Close out the Transformer-architecture milestone by reviewing its tensor
semantics, checking documentation accuracy, and confirming the completed model
still passes its focused verification suite.

## Concepts Reviewed

- Distinguished token IDs `(batch, sequence)` from token embeddings and hidden
  states `(batch, sequence, d_model)`.
- Distinguished the current `sequence_length` from the configured maximum
  `context_length`.
- Traced RoPE token positions, trigonometric buffers, feature-pair rotation,
  broadcasting, and reconstruction with `stack(...).flatten(...)`.
- Clarified why splitting heads requires reshape followed by transpose: reshape
  changes grouping, while transpose changes axis order.
- Traced attention masking before and after softmax, including the fully masked
  row safeguard.
- Identified CPU as the appropriate device for component development and small
  smoke tests; Apple MPS becomes useful when the end-to-end training loop is
  ready for substantive runs.

## Documentation Updates

- Updated the project-status audit date.
- Expanded `PORTFOLIO.md` from the early component list to the verified complete
  Transformer architecture.
- Corrected portfolio positioning so training, checkpointing, and generation
  are clearly described as future work rather than completed functionality.
- Rechecked `README.md` and `EXPERIMENTS.md`; their current milestone, measured
  results, and next-step descriptions required no further changes.

## Verification

```text
pytest -q tests/test_model.py: 13 passed
ruff check cs336_basics/model.py cs336_basics/nn_utils.py tests/adapters.py: passed
```

## Next Task

Implement numerically stable cross-entropy, then continue with the remaining
training utilities.
